# ═══════════════════════════════════════════════════════════
#  audit-service — Deployment + Service
#
#  ⚠️ Image locale : sur Minikube/Kind, build puis charge l'image :
#    docker build -t docker-audit-service:latest .
#    minikube image load docker-audit-service:latest
#  Sur un cluster cloud, remplace l'image par l'URL de ton
#  registre (voir README, section Cloud) et mets imagePullPolicy: Always.
#
#  JAVA_OPTS=-XX:MaxRAMPercentage=75.0 : contraint la heap JVM à
#  75% de la limite mémoire du conteneur, cohérent avec le
#  Dockerfile (ENTRYPOINT lit $JAVA_OPTS). Sans ça, la JVM peut
#  mal évaluer la mémoire disponible et se faire tuer (OOMKilled).
#
#  Limite CPU à "1" (pas 500m) : Spring Boot est gourmand en CPU
#  au démarrage (JIT, chargement de classes) ; une limite trop
#  basse provoque un throttling sévère qui fait échouer les
#  probes de liveness en boucle avant même que l'appli ait fini
#  de démarrer.
#
#  startupProbe généreuse (5 min de budget) : laisse le temps à
#  la JVM de démarrer sous charge partagée sans être tuée
#  prématurément — readiness/liveness ne s'activent qu'après.
# ═══════════════════════════════════════════════════════════
apiVersion: apps/v1
kind: Deployment
metadata:
  name: audit-service
  namespace: juribook
  labels:
    app: audit-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: audit-service
  template:
    metadata:
      labels:
        app: audit-service
    spec:
      initContainers:
        - name: wait-for-rdb
          image: busybox:1.37
          command: ["sh", "-c", "until nc -z 172.16.0.2 5432; do echo waiting for RDB 172.16.0.2:5432; sleep 2; done"]
      containers:
        - name: audit-service
          image: rg.fr-par.scw.cloud/juribook/audit-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8085
          env:
            - name: JAVA_OPTS
              value: "-XX:MaxRAMPercentage=75.0"
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:postgresql://172.16.0.2:5432/auditdb?sslmode=require"
            - name: SPRING_DATASOURCE_USERNAME
              valueFrom:
                secretKeyRef:
                  name: postgres-credentials
                  key: POSTGRES_USER
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-credentials
                  key: POSTGRES_PASSWORD
            - name: SPRING_KAFKA_BOOTSTRAP_SERVERS
              valueFrom:
                configMapKeyRef:
                  name: juribook-common-config
                  key: SPRING_KAFKA_BOOTSTRAP_SERVERS
          startupProbe:
            httpGet:
              path: /actuator/health
              port: 8085
            periodSeconds: 10
            failureThreshold: 30
            timeoutSeconds: 5
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8085
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8085
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 3
          resources:
            requests:
              cpu: 150m
              memory: 256Mi
            limits:
              cpu: "1"
              memory: 512Mi

---
apiVersion: v1
kind: Service
metadata:
  name: audit-service
  namespace: juribook
spec:
  selector:
    app: audit-service
  ports:
    - port: 8085
      targetPort: 8085
  type: ClusterIP
